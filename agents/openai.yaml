interface:
  display_name: "RAGFlow Dataset Ingest"
  short_description: "Upload docs and track RAGFlow parsing"
  default_prompt: "Use $ragflow-dataset-ingest to upload documents, start parsing, and monitor parser status in RAGFlow."
