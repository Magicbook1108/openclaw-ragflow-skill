interface:
  display_name: "RAGFlow Dataset And Retrieval"
  short_description: "Manage datasets, docs, parsing, and retrieval"
  default_prompt: "Use $ragflow-dataset-ingest for RAGFlow dataset ops, document ops, parse start or stop, parser-status checks, and retrieval. Require explicit document IDs for document delete or stop actions."
